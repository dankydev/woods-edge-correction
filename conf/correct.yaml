# MODEL
channels: 3
replaceMaxpoolWithStride: true # uses stride instead of max-pooling to collapse data into the final feature map
modelsDir: '../models/'
correctModel: '2021-11-12-01-54-50_vgg_dss_unet_maxshift15_minshift0_mse0_vgglow1_msssim0_vgghigh1_dists0_dss1_fsim0_lr0.0002_batch8_epochs100_freq100_replaceWithStrideTrue.pth'
device: 'cpu'

# CORRECTION
pathToCorrect: '../dataset/generated_woods_for_test/old'
portionSize: 128